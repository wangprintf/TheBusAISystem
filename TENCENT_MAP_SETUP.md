# 腾讯地图接入说明

地图页面已经接入腾讯地图 GL JS API；还需要你申请并填写一个 **Web 端 Key** 才会显示真实地图。

## 你需要做的操作

1. 打开腾讯位置服务控制台：<https://lbs.qq.com/>，用腾讯云账号登录并完成认证或计费配置（以控制台当前要求为准）。
2. 在「应用管理 → 我的应用」中新建应用，并添加一个 **Web 端（JavaScript API）** Key。
3. 为 Key 配置允许访问的域名：
   - 本机测试可添加 `localhost` 和你实际使用的本机地址；
   - 上线后添加网站域名，例如 `map.example.com`；
   - 不要使用无限制白名单。Web Key 会随网页发送给浏览器，因此必须用域名限制它。
4. 打开 `js/config.js`，在 `key: ''` 的单引号中粘贴新 Key。
5. 不要直接双击打开 `index.html`。在项目目录启动本地静态服务后，通过浏览器访问它，例如：

```powershell
npx serve .
```

若电脑没有 Node.js，请先安装 Node.js LTS，再重新打开终端执行上面的命令。启动后访问命令行显示的本地地址；通常是 `http://localhost:3000`。

## 现有页面已完成的功能

- 真实腾讯底图；
- 根据告警数据里的 `longitude` / `latitude` 绘制高、中优先级点位；
- 绘制巡检路线；
- 右侧图层开关会筛选对应类型的点位；
- 点击地图点位或右侧告警可打开现有事件详情。

## 接真实后端时的数据要求

`/alerts` 返回的每条告警至少应包含：`id`、`category`、`severity`、`longitude`、`latitude`。经纬度必须为数值；腾讯地图使用 GCJ-02 坐标系。若设备上报的是 GPS/WGS84 坐标，应由后端先转换，否则点位会出现偏移。
